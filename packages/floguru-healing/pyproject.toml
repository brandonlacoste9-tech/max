[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "floguru-healing"
version = "0.1.0"
description = "HyperHealing — self-improvement engine that learns from past executions"
requires-python = ">=3.10"
dependencies = [
    "floguru-shared",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.23"]
