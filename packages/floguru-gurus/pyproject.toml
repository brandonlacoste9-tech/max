[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "floguru-gurus"
version = "0.1.0"
description = "AI Reasoning Guru agents — specialised LLM-powered workers for the FloGuru platform"
requires-python = ">=3.10"
dependencies = [
    "floguru-shared",
    "litellm>=1.30",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.23"]
