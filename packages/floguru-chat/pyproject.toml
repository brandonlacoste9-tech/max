[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "floguru-chat"
version = "0.1.0"
description = "Chat gateway — Telegram, WhatsApp, and Discord integrations for FloGuru"
requires-python = ">=3.10"
dependencies = [
    "floguru-shared",
    "httpx>=0.27",
]

[project.optional-dependencies]
telegram = ["python-telegram-bot>=21.0"]
discord = ["discord.py>=2.3"]
all = ["python-telegram-bot>=21.0", "discord.py>=2.3"]
dev = ["pytest>=7.0", "pytest-asyncio>=0.23"]
