[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "floguru-browser"
version = "0.1.0"
description = "Browser automation Guru — headless browser control for end-to-end workflows"
requires-python = ">=3.10"
dependencies = [
    "floguru-shared",
    "playwright>=1.40",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.23"]
